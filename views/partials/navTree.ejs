<% if(locals.tree){ %>
<nav class="folder-tree">
  <% tree.map((folder,index)=>{ %> <% if(index===0){ %>

  <a href="/upload/<%=folder.id %>" class="root-folder tree-link"
    ><%=user.username %>'s Drive</a
  >
  <% }else{ %>
  <img src="/icons/arrow-right.svg" alt="right arrow" />
  <a href="<%=folder.id %>" class="tree-link"><%=folder.folderName %></a>
  <% }}) %>
</nav>
<% } %>
